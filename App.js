import { StatusBar, SafeAreaView, View } from 'expo-status-bar';
import React from 'react';
import { View } from 'react-native';
import Projetos from './src/telas/Projetos';

export default function App() {
    return ( <
        View >
        <
        Projetos / >
        <
        StatusBar style = "auto" / >
        <
        /View>
    );
}